/**
 * Berget Design System - Layout Components
 * 
 * Layout primitives for consistent structure and spacing.
 * These components handle page structure, content boundaries, and element spacing.
 */

export { Container, containerVariants, type ContainerProps } from './Container'
export { Stack, stackVariants, type StackProps } from './Stack'
